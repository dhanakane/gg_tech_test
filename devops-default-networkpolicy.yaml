apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: devops-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/devops-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/devops-default: "true"
